# name: e2e

# on:
#   workflow_dispatch:
#   push:
#     branches: [main]

# concurrency:
#   group: e2e-${{ github.ref }}
#   cancel-in-progress: true


# jobs:
#   e2e:
#     runs-on: ubuntu-22.04
#     permissions: { id-token: write, contents: read }
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       # OIDC auth to GCP (only needed if your e2e needs GCP access)
#       - name: Auth to GCP
#         uses: google-github-actions/auth@v2
#         with:
#           workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
#           service_account: ${{ secrets.WIF_SERVICE_ACCOUNT }}

#       - name: Set up gcloud
#         uses: google-github-actions/setup-gcloud@v2
#         with:
#           project_id: porfolio-480111

#       # If you pull images from Artifact Registry during e2e
#       - name: Docker auth to GAR
#         run: gcloud auth configure-docker us-central1-docker.pkg.dev

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Run e2e tests
#         run: |
#           set -euxo pipefail
#           chmod +x scripts/e2e_check.sh || true
#           bash scripts/e2e_check.sh
